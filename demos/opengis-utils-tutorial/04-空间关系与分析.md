# 空间关系与分析

## 概述

空间关系分析是GIS的核心功能之一。通过判断几何对象之间的空间关系，可以实现空间查询、叠加分析、选址分析等常见GIS功能。本章介绍如何使用JTS和ESRI两种引擎进行空间关系分析。

## 基本空间关系

### 相交（Intersects）

两个几何对象有任意公共部分时，称为相交：

```java
/**
 * 判断几何是否相交
 */
public static boolean intersects(Geometry a, Geometry b) {
    return a.intersects(b);
}
```

**应用场景**：
- 空间查询：找出与指定区域相交的所有要素
- 碰撞检测：判断两个对象是否发生空间重叠
- 路线规划：判断路线是否经过某个区域

### 相离（Disjoint）

两个几何对象没有任何公共点时，称为相离：

```java
/**
 * 判断几何是否相离
 */
public static boolean disjoint(Geometry a, Geometry b) {
    return a.disjoint(b);
}
```

**关系说明**：`disjoint`与`intersects`互为反义，即：
```java
disjoint(a, b) == !intersects(a, b)
```

### 接触（Touches）

两个几何对象有公共点但没有公共内部区域时，称为接触：

```java
/**
 * 判断几何是否接触
 */
public static boolean touches(Geometry a, Geometry b) {
    return a.touches(b);
}
```

**典型情况**：
- 两个相邻地块共享边界
- 道路与地块边界相接
- 点落在线或面的边界上

### 交叉（Crosses）

两个几何对象的内部有公共部分，但既不相等也不包含：

```java
/**
 * 判断几何是否交叉
 */
public static boolean crosses(Geometry a, Geometry b) {
    return a.crosses(b);
}
```

**典型情况**：
- 道路穿过区域（线与面交叉）
- 两条道路相交（线与线交叉）

### 包含（Contains）与被包含（Within）

```java
/**
 * 判断A是否包含B
 */
public static boolean contains(Geometry a, Geometry b) {
    return a.contains(b);
}

/**
 * 判断A是否在B内部
 */
public static boolean within(Geometry a, Geometry b) {
    return a.within(b);
}
```

**关系说明**：
```java
contains(a, b) == within(b, a)
```

**应用场景**：
- 判断点是否在某个区域内
- 判断地块是否完全位于行政区划内
- 空间过滤：只保留完全落入指定范围的要素

### 重叠（Overlaps）

两个同维度的几何对象有公共区域，但又不完全相同：

```java
/**
 * 判断几何是否重叠
 */
public static boolean overlaps(Geometry a, Geometry b) {
    return a.overlaps(b);
}
```

**说明**：重叠只发生在同维度几何之间（面与面、线与线）。

## 距离计算

### 最短距离

```java
/**
 * 计算几何之间的最短距离
 */
public static double distance(Geometry a, Geometry b) {
    return a.distance(b);
}
```

**注意**：返回值单位与坐标系单位一致：
- 地理坐标系返回度
- 投影坐标系返回米

### 距离判断

```java
/**
 * 判断几何间距离是否小于给定值
 */
public static boolean isWithinDistance(Geometry a, Geometry b, double distance) {
    return a.isWithinDistance(b, distance);
}
```

**应用场景**：
- 近邻搜索：查找指定距离内的设施
- 缓冲区分析：判断要素是否在影响范围内
- 预警系统：判断两个目标是否过于接近

## DE-9IM模型

DE-9IM（Dimensionally Extended 9-Intersection Model）是空间关系的标准描述模型，提供更精确的空间关系判断。

### 关系矩阵

```
         Interior  Boundary  Exterior
Interior    II        IB        IE
Boundary    BI        BB        BE
Exterior    EI        EB        EE
```

每个位置的值表示两个几何对应部分的交集维度：
- `T`：有交集（任意维度）
- `F`：无交集
- `0`：交集为点
- `1`：交集为线
- `2`：交集为面
- `*`：任意（不关心）

### 获取关系矩阵

```java
/**
 * 获取几何关系矩阵
 */
public static String relate(Geometry a, Geometry b) {
    return a.relate(b).toString();
}
```

### 模式匹配

```java
/**
 * 判断几何是否符合给定关系模式
 */
public static boolean relatePattern(Geometry a, Geometry b, String pattern) {
    return a.relate(b, pattern);
}
```

**常用模式**：

| 关系 | 模式 |
|-----|------|
| 内部相交 | `T********` |
| 边界相交 | `*T*******` |
| A完全包含B | `T*****FF*` |
| 相邻（共享边界） | `FF*FT****` |

## 几何相等判断

### 精确相等

```java
/**
 * 结构完全相同（节点顺序相同）
 */
public static boolean equalsExact(Geometry a, Geometry b) {
    return a.equalsExact(b);
}

/**
 * 带容差的结构相等
 */
public static boolean equalsExactTolerance(Geometry a, Geometry b, double tolerance) {
    return a.equalsExact(b, tolerance);
}
```

### 规范化相等

```java
/**
 * 规范化后比较（忽略节点顺序）
 */
public static boolean equalsNorm(Geometry a, Geometry b) {
    return a.equalsNorm(b);
}
```

### 拓扑相等

```java
/**
 * 拓扑相等（表示相同的点集）
 */
public static boolean equalsTopo(Geometry a, Geometry b) {
    return a.equalsTopo(b);
}
```

**区别说明**：
- `equalsExact`：节点顺序、数量都必须完全相同
- `equalsNorm`：规范化后比较，忽略节点顺序
- `equalsTopo`：只比较几何表示的空间是否相同

## ESRI几何引擎

除了JTS，还可以使用ESRI几何引擎，其API基于WKT字符串：

```java
/**
 * 使用ESRI引擎判断相交
 */
public static boolean intersects(String awkt, String bwkt, Integer wkid) {
    Geometry a = EsriGeometryUtil.createGeometryByWkt(awkt);
    Geometry b = EsriGeometryUtil.createGeometryByWkt(bwkt);
    SpatialReference sr = SpatialReference.create(wkid);
    return OperatorIntersects.local().execute(a, b, sr, null);
}

/**
 * 使用ESRI引擎判断包含
 */
public static boolean contains(String awkt, String bwkt, Integer wkid) {
    Geometry a = EsriGeometryUtil.createGeometryByWkt(awkt);
    Geometry b = EsriGeometryUtil.createGeometryByWkt(bwkt);
    SpatialReference sr = SpatialReference.create(wkid);
    return OperatorContains.local().execute(a, b, sr, null);
}

/**
 * 使用ESRI引擎计算距离
 */
public static double distance(String awkt, String bwkt, Integer wkid) {
    Geometry a = EsriGeometryUtil.createGeometryByWkt(awkt);
    Geometry b = EsriGeometryUtil.createGeometryByWkt(bwkt);
    SpatialReference sr = SpatialReference.create(wkid);
    return OperatorDistance.local().execute(a, b, sr);
}
```

**选择建议**：
- JTS：内存中的几何对象操作，性能更好
- ESRI：基于WKT字符串操作，接口更简洁，某些算法更健壮

## 空间计算操作

### 缓冲区（Buffer）

```java
/**
 * 计算缓冲区
 */
public static Geometry buffer(Geometry geom, double distance) {
    return geom.buffer(distance);
}

// ESRI引擎
public static String buffer(String wkt, Integer wkid, double distance) {
    Geometry geom = EsriGeometryUtil.createGeometryByWkt(wkt);
    SpatialReference sr = SpatialReference.create(wkid);
    Geometry buffer = OperatorBuffer.local().execute(geom, sr, distance, null);
    return EsriGeometryUtil.toWkt(buffer);
}
```

### 交集（Intersection）

```java
/**
 * 计算交集
 */
public static Geometry intersection(Geometry a, Geometry b) {
    return a.intersection(b);
}
```

### 并集（Union）

```java
/**
 * 计算并集
 */
public static Geometry union(Geometry a, Geometry b) {
    return a.union(b);
}
```

### 差集（Difference）

```java
/**
 * 计算差集（A减去B）
 */
public static Geometry difference(Geometry a, Geometry b) {
    return a.difference(b);
}
```

### 对称差集（Symmetric Difference）

```java
/**
 * 计算对称差集
 */
public static Geometry symDifference(Geometry a, Geometry b) {
    return a.symDifference(b);
}
```

### 凸包（Convex Hull）

```java
/**
 * 计算凸包
 */
public static Geometry convexHull(Geometry geom) {
    return new ConvexHull(geom).getConvexHull();
}
```

### 几何简化

```java
/**
 * 简化几何（Douglas-Peucker算法）
 */
public static Geometry simplify(Geometry geom, double tolerance) {
    return DouglasPeuckerSimplifier.simplify(geom, tolerance);
}
```

## 实践案例

### 案例1：空间查询

```java
/**
 * 空间查询：找出与指定区域相交的要素
 */
public List<OguFeature> spatialQuery(OguLayer layer, String queryWkt) {
    List<OguFeature> result = new ArrayList<>();
    Geometry queryGeom = GeometryConverter.wkt2Geometry(queryWkt);
    
    for (OguFeature feature : layer.getFeatures()) {
        Geometry featureGeom = GeometryConverter.wkt2Geometry(feature.getWkt());
        if (featureGeom.intersects(queryGeom)) {
            result.add(feature);
        }
    }
    return result;
}
```

### 案例2：点在面内判断

```java
/**
 * 判断点是否在面内
 */
public boolean isPointInPolygon(double x, double y, String polygonWkt) {
    GeometryFactory factory = new GeometryFactory();
    Point point = factory.createPoint(new Coordinate(x, y));
    Geometry polygon = GeometryConverter.wkt2Geometry(polygonWkt);
    
    // contains严格在内部，intersects包含边界
    return polygon.contains(point);
}
```

### 案例3：查找相邻要素

```java
/**
 * 查找与目标要素相邻的所有要素
 */
public List<OguFeature> findAdjacentFeatures(OguLayer layer, OguFeature target) {
    List<OguFeature> result = new ArrayList<>();
    Geometry targetGeom = GeometryConverter.wkt2Geometry(target.getWkt());
    
    for (OguFeature feature : layer.getFeatures()) {
        if (feature.getWfId().equals(target.getWfId())) {
            continue;  // 跳过自己
        }
        
        Geometry featureGeom = GeometryConverter.wkt2Geometry(feature.getWkt());
        
        // 相邻：接触但不重叠
        if (featureGeom.touches(targetGeom)) {
            result.add(feature);
        }
    }
    return result;
}
```

### 案例4：缓冲区范围查询

```java
/**
 * 查找指定位置周边一定距离内的要素
 */
public List<OguFeature> bufferQuery(String centerWkt, double distance, 
        OguLayer layer) {
    List<OguFeature> result = new ArrayList<>();
    
    // 转换到投影坐标系计算缓冲区
    int projWkid = CrsUtil.getProjectedWkid(CrsUtil.getDh(centerWkt));
    String projWkt = CrsUtil.transform(centerWkt, 4490, projWkid);
    
    // 创建缓冲区
    Geometry center = GeometryConverter.wkt2Geometry(projWkt);
    Geometry buffer = center.buffer(distance);
    
    // 转回地理坐标系
    buffer = CrsUtil.transform(buffer, projWkid, layer.getWkid());
    
    // 查询相交要素
    for (OguFeature feature : layer.getFeatures()) {
        Geometry featureGeom = GeometryConverter.wkt2Geometry(feature.getWkt());
        if (featureGeom.intersects(buffer)) {
            result.add(feature);
        }
    }
    return result;
}
```

### 案例5：叠加分析

```java
/**
 * 两个图层的叠加分析
 */
public OguLayer overlayAnalysis(OguLayer layer1, OguLayer layer2) {
    OguLayer result = new OguLayer();
    result.setYwName("overlay_result");
    result.setWkid(layer1.getWkid());
    
    List<OguFeature> features = new ArrayList<>();
    
    for (OguFeature f1 : layer1.getFeatures()) {
        Geometry g1 = GeometryConverter.wkt2Geometry(f1.getWkt());
        
        for (OguFeature f2 : layer2.getFeatures()) {
            Geometry g2 = GeometryConverter.wkt2Geometry(f2.getWkt());
            
            if (g1.intersects(g2)) {
                Geometry intersection = g1.intersection(g2);
                
                if (!intersection.isEmpty() && intersection.getArea() > 0) {
                    OguFeature feature = new OguFeature();
                    feature.setWfId(f1.getWfId() + "_" + f2.getWfId());
                    feature.setWkt(intersection.toText());
                    features.add(feature);
                }
            }
        }
    }
    
    result.setFeatures(features);
    return result;
}
```

## 性能优化

### 空间索引

对于大数据量的空间查询，使用空间索引可以显著提升性能：

```java
// 构建R树索引
STRtree index = new STRtree();
for (OguFeature feature : layer.getFeatures()) {
    Geometry geom = GeometryConverter.wkt2Geometry(feature.getWkt());
    index.insert(geom.getEnvelopeInternal(), feature);
}
index.build();

// 使用索引查询
Geometry queryGeom = GeometryConverter.wkt2Geometry(queryWkt);
List<?> candidates = index.query(queryGeom.getEnvelopeInternal());

// 对候选要素进行精确判断
for (Object obj : candidates) {
    OguFeature feature = (OguFeature) obj;
    Geometry featureGeom = GeometryConverter.wkt2Geometry(feature.getWkt());
    if (featureGeom.intersects(queryGeom)) {
        // 真正相交的要素
    }
}
```

### 外包矩形预筛选

```java
// 先判断外包矩形
if (!geomA.getEnvelopeInternal().intersects(geomB.getEnvelopeInternal())) {
    return false;  // 外包矩形不相交，几何必定不相交
}
// 再进行精确判断
return geomA.intersects(geomB);
```

## 小结

本章介绍了空间关系分析的核心内容：

1. **基本空间关系**：相交、相离、接触、交叉、包含、重叠
2. **距离计算**：最短距离和距离判断
3. **DE-9IM模型**：精确的空间关系描述
4. **空间计算**：缓冲区、交集、并集、差集等操作
5. **性能优化**：空间索引和外包矩形预筛选

下一章将介绍Shapefile数据处理，学习如何读写这种最常用的GIS数据格式。
